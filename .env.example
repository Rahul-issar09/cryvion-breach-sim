# Copy to .env and adjust. .env is gitignored.
# ---- Vulnerable DB (intentionally weak for the sim) ----
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=cryvion
MYSQL_USER=webapp
MYSQL_PASSWORD=webapp123

# ---- Flask app ----
FLASK_SECRET=insecure-dev-secret
APP_PORT=8080

# ---- Internal SSH target (weak creds for lateral-movement demo) ----
SSH_USER=svc-backup
SSH_PASSWORD=Password1

# ---- Wazuh dashboard ----
WAZUH_DASHBOARD_USER=admin
WAZUH_DASHBOARD_PASSWORD=SecretPassword
