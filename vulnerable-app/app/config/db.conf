# CryvionPortal datasource config  (INTENTIONALLY exposed — T1552.001 Credentials in Files)
# Discovered by the attacker post-RCE to enable lateral movement.
[database]
host = db
port = 3306
name = cryvion
user = webapp
password = webapp123

[backup]
# service account reused on the internal backup host (credential reuse -> lateral movement)
ssh_host = internal-svc
ssh_user = svc-backup
ssh_password = Password1
